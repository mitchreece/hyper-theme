const { colors } = require('./vars/')

module.exports = `
    .tabs_title {
        color: ${ colors.secondary };
    }

    .tab_active {
        color: ${ colors.secondary };
    }

    .tab_icon {
        color: ${ colors.secondary };
    }
`